.. _installation:

============
Installation
============

* Install the development version::

    pip install django-teams

* Add ``teams`` to your ``INSTALLED_APPS`` setting::

    INSTALLED_APPS = (
        # ...
        "teams",
        # ...
    )


.. _dependencies:

Dependencies
============

